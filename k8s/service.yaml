apiVersion: v1
kind: Service
metadata:
  name: notesbhej-api
  namespace: notesbhej
  labels:
    app.kubernetes.io/name: notesbhej-api
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: notesbhej-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
